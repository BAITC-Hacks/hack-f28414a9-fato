services:
  autoprotocol:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - model-cache:/root/.cache/huggingface
    restart: unless-stopped
volumes:
  model-cache:
